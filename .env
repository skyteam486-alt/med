# Supabase public client credentials for the `med-monitoring` project.
# The anon/publishable key is a PUBLIC client key (shipped to the browser) and is
# safe to commit — all data access is protected by Row Level Security.
# Put any real secrets in .env.local (gitignored), never here.
NEXT_PUBLIC_SUPABASE_URL=https://xlbxsyznlrezbuerqohs.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_hZCpg3RWxr-ug6oKYOf0WQ_T6BC_CjS
